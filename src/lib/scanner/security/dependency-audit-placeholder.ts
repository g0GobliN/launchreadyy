/**
 * Dependency audit placeholder — CI audit pack + OSV aggregation.
 * npm audit CI lives in dependency-audit.ts; OSV live lookup in osv-deps.ts.
 */
export { checkDependencyAudit } from "./dependency-audit";
export { checkOsvDependencies } from "./osv-deps";
